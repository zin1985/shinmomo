; Runtime probe plan / pseudo-assembly notes
; Patch or hook one controlled object/script byte at a time.
; Log:
;   slot deltas $0619/$0719/$0759/$0799
;   active status
;   OAM submit count
;   text/display trigger
; Then classify opcode behavior.
