# shinmomo-analysis vol028 exact supplemental package

This package contains the exact file layout requested in the thread:

- `analysis/*.md`
- `data/*.csv`
- `code/*.py` / `code/*.asm`
- `docs/*.md`
- `notes/*.md`

No ROM, no old ZIP, no graphics reconstruction binaries are included.

The contents are written as a supplemental handoff after the previous vol017 diff ZIP. Several late-run VM/AI claims are preserved as hypotheses rather than hard facts.
